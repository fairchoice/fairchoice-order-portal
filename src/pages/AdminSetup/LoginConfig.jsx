// Backward-compatible module name for older internal routes.
// Staff credentials, roles, page access and important functions now share the
// centralized Access Control implementation.
export { default } from "./AccessControl";
